<h1>This is edit template <%= params[:id]%> <%= link_to "back", index_path%></h1>

<div class="form">
	
	<%= form_for @post do |f| %>
		<%= f.label :title %><br>
		<%= f.text_field :title %><br>

		<%= f.label :body %><br>
		<%= f.text_area :body %><br>
		<br>
		<%= f.button%>
	<% end %>

</div>